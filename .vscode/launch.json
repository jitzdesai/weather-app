{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Weather App",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/weather_app.py",
      "python": "${workspaceFolder}/.venv/bin/python",
      "preLaunchTask": "weather-app: prepare environment",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
